import React, { Component } from 'react';
import { View, Text } from 'react-native';

class Time extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text> Time </Text>
      </View>
    );
  }
}

export default Time;
