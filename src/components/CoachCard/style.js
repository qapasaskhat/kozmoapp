import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container:{
    },
    img:{
        width: 24,
          height: 24,
          resizeMode: 'contain',
    }
})

export default styles